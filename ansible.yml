---
  hosts: all
  become: true
  tasks:
    - name: Run Deployment
      command: kubectl apply -f /home/ubuntu/Deployment.yaml

    - name: Run Service
      command: kubectl apply -f /home/ubuntu/Service.yaml 